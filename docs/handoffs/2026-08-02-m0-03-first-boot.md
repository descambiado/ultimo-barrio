# Handoff de M0-03 — primer boot

## Contexto

- Fecha: 2026-08-02.
- Rama: `feat/m0-bootstrap`.
- Hito: M0-03 — crear `main.scene` y validar el primer boot jugable.
- Estado del hito: **COMPLETADO** el 2026-08-03.
- Licencia vigente: Mozilla Public License 2.0
  (`SPDX-License-Identifier: MPL-2.0`).
- Commit de implementación: `2ba1925` (`feat(scene): add first boot scene`).

## Resultado comprobado

- Existe `Assets/scenes/main.scene` y el proyecto la usa como escena de inicio.
- La jerarquía contiene los roots `World`, `Systems`, `SpawnPoints` y `Debug`.
- `World` contiene suelo provisional, luz direccional y cielo.
- `SpawnPoints` contiene un punto de aparición provisional.
- `Systems` contiene una cámara y una instancia del prefab oficial
  `Player Controller` de s&box.
- Los ensamblados de runtime y editor compilaron con 0 errores y 0 avisos.
- Play Mode inició y se detuvo correctamente en `main.scene`.
- La consulta de consola no devolvió errores del proyecto durante la prueba.
- El jugador apareció visible y la cámara produjo una imagen válida. La
  evidencia está en `docs/media/first-boot.png`.
- El operador comprobó input real con WASD, salto y ratón, además del cambio
  entre tercera y primera persona.
- La lectura posterior confirmó 10 compiladores con 0 errores/avisos, ninguna
  entrada `Error` y ningún aviso filtrado por `ultimo_barrio` entre 53 entradas.
- Play Mode se detuvo sin dejar cambios de escena sin guardar.

La captura demuestra instancia y render. El movimiento y la cámara se apoyan
en la observación manual registrada; el MCP solo devuelve el transform
serializado y no debe presentarse como telemetría física runtime.

## Fuera de este hito

- Ownership y movimiento independiente en una sesión con dos clientes.
- Conexión y desconexión de una segunda instancia.
- Consola separada de host y cliente durante esa prueba.

Estas comprobaciones corresponden a M0-04. El MCP inspecciona la escena,
controla Play Mode y lee la consola, pero no abre ni controla el segundo
proceso.

## Estado de GitHub

- M0-02 está completado.
- Owner real: `descambiado`, confirmado por GitHub CLI.
- Repositorio público: https://github.com/descambiado/ultimo-barrio
- `origin`: `https://github.com/descambiado/ultimo-barrio.git`.
- Publicados: `main`, `feat/m0-bootstrap` y `bootstrap-v0.0.0`.
- Draft PR: https://github.com/descambiado/ultimo-barrio/pull/7
- Issue de M0-03: https://github.com/descambiado/ultimo-barrio/issues/1
- Issues posteriores: #2 para dos clientes, #3 para el apartamento y #4–#6
  para los spikes iniciales.

## Ruta técnica verificada para M0-04

La instalación oficial local confirma el flujo de prueba con dos instancias:

- El menú del viewport usa `EditorUtility.Network.StartHosting` para iniciar
  hosting.
- `Join via new instance` lanza `sbox.exe -joinlocal +instanceid <N>` y puede
  añadir `-sw -720` para una ventana pequeña.
- La documentación oficial también admite `connect local` y `reconnect`.

Fuentes locales consultadas:

```text
C:\Program Files (x86)\Steam\steamapps\common\sbox\addons\tools\Code\Scene\SceneView\ViewportTools.Network.cs
C:\Program Files (x86)\Steam\steamapps\common\sbox\addons\base\code\Components\Networking\NetworkHelper.cs
```

`NetworkHelper` crea el lobby, clona `PlayerPrefab` para cada conexión y llama
a `NetworkSpawn(connection)`. La escena actual no contiene ese componente y
mantiene un Player directo. Añadir ambos a la vez puede duplicar el jugador del
host, por lo que M0-04 debe convertir el Player en prefab de spawn antes de
lanzar otra instancia.

El lanzamiento está documentado, pero la prueba no se ejecutó: el MCP no
expone el menú de hosting, input independiente ni la consola/cámara del segundo
proceso. No existe evidencia todavía de dos jugadores, ownership, movimiento
independiente o desconexión limpia.

## Archivos relevantes

- `Assets/scenes/main.scene`: escena fuente del primer boot.
- `Assets/scenes/main.scene_d`: sidecar generado por s&box para la escena.
- `ultimo_barrio.sbproj`: configura `scenes/main.scene` como escena de inicio.
- `docs/media/first-boot.png`: captura real tomada durante Play Mode.
- `BACKLOG.md`: conserva abiertos los criterios no verificados.
- `CHANGELOG.md`: registra la escena y el alcance exacto de la prueba.
- `docs/planning/m1-01-apartment-claim.md`: contrato y estrategia preparada,
  sin implementación.

## Cómo repetir la prueba local

1. Abrir `ultimo_barrio.sbproj` en s&box.
2. Confirmar que `scenes/main.scene` es la escena activa.
3. Inspeccionar los roots `World`, `Systems`, `SpawnPoints` y `Debug`.
4. Comprobar que runtime y editor indican 0 errores y 0 avisos.
5. Iniciar Play Mode y confirmar que el jugador aparece sobre el suelo y la
   cámara renderiza la escena.
6. Leer la consola y confirmar que no aparecen errores del proyecto.
7. Detener Play Mode y comprobar que la escena no queda con cambios inesperados.

M0-03 queda cerrado con la prueba manual del 2026-08-03. La prueba con dos
clientes corresponde al siguiente bloque y no se ha ejecutado.

## Riesgos y bloqueadores reales

- La sesión multijugador todavía no está configurada ni probada.
- La escena mantiene un Player directo; añadir `NetworkHelper` sin convertirlo
  en prefab de spawn duplicaría el jugador del host.
- La segunda instancia requerirá interacción/inspección fuera del MCP si no es
  posible leer su estado mediante el proceso principal.

## Próximas tres acciones

1. Configurar spawn autoritativo con `NetworkHelper` sin duplicar el Player.
2. Iniciar hosting y lanzar una segunda instancia local con el comando oficial.
3. Comprobar conexión, ownership, movimiento/cámara independientes,
   desconexión y ambas consolas.
